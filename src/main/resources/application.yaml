server:
  port: ${PORT}
  servlet:
    context-path: /api

spring:
  application:
    name: spring-barber
  jpa:
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQL95Dialect
  datasource:
    url: ${DATABASE_URL}
